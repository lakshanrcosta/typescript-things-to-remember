const stringValue: string = 'This id a string';

const stringConcat: string = 'Added string ' + stringValue;

stringConcat.split(' ');

// Math.round(stringConcat);
// Argument of type 'string' is not assignable to parameter of type 'number'.
